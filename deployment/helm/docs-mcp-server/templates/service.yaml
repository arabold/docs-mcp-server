apiVersion: v1
kind: Service
metadata:
  name: {{ include "docs-mcp-server.fullname" . }}
  labels:
    {{- include "docs-mcp-server.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "docs-mcp-server.selectorLabels" . | nindent 4 }}
